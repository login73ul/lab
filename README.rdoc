rails new lab --database=postgres
# lab/config/database.yml redacted developers
rake db:create
# look pgAdmin and see new database
#use scaffold
rails g scaffold news title:string author:string article:text
# use rake -T for print list rake
rake db:migrate
rake db:schema:load
